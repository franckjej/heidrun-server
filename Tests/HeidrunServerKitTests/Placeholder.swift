import Testing
@testable import HeidrunServerKit

@Suite("Placeholder")
struct PlaceholderTests {
    @Test("package builds and imports HeidrunServerKit")
    func placeholder() {
        #expect(Bool(true))
    }
}
