import HeidrunServerKit

@main
struct HeidrunServerExecutable {
    static func main() async {
        // Real entry point lands in Task 10. For now this just keeps the
        // executable target compiling.
        print("HeidrunServer placeholder — see main.swift Task 10")
    }
}
