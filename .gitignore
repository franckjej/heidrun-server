.build/
.swiftpm/
*.xcodeproj
xcuserdata/
DerivedData/
.DS_Store
.netrc

# Local-only Claude / MCP config (per-checkout)
.claude/
.mcp.json

# Local TLS test certs / Let's Encrypt copies
/tls/
*.pem
!Tests/**/TestCerts/*.pem
